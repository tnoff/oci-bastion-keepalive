{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "gitlab>tnoff-projects/github-workflows//renovate/default",
    "gitlab>tnoff-projects/github-workflows//renovate/python"
  ],
  "enabledManagers": ["gitlabci", "gitlabci-include", "pep621", "custom.regex"],
  "packageRules": [
    {
      "description": "oci-cli pins the oci SDK to an exact version (e.g. oci-cli==3.89.0 requires oci==2.181.0), so the two must upgrade together in one MR — a lone oci bump produces an unresolvable pip set.",
      "matchManagers": ["pep621"],
      "matchPackageNames": ["oci", "oci-cli"],
      "groupName": "oci python packages"
    }
  ]
}
